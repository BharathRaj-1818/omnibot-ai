# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# AI & NLP
transformers==4.35.2
torch==2.1.1
sentencepiece==0.1.99
protobuf==4.25.1

# Speech Processing
openai-whisper==20231117
gtts==2.5.0
pydub==0.25.1

# Image Generation (optional — only needed if ImageService is enabled)
diffusers==0.24.0
accelerate==0.25.0

# Database — FIXED: added aiosqlite for async SQLAlchemy support
sqlalchemy==2.0.23
alembic==1.13.0
aiosqlite==0.19.0          # Required for async SQLite engine
asyncpg==0.29.0            # Required for async PostgreSQL (production)

# Language Detection — ADDED: used by TranslationService
langdetect==1.0.9

# Platform Integrations
python-telegram-bot==20.7
discord.py==2.3.2

# Utilities
python-multipart==0.0.6    # Required for file uploads (audio endpoint)
python-dotenv==1.0.0
aiofiles==23.2.1
requests==2.31.0
httpx==0.25.2              # ADDED: async HTTP client

# Logging
loguru==0.7.2